You should run `npm run lint -- --fix` to automatically fix lint errors after Edit, MultiEdit, Write or Update tool call

if they are not fixed, you can manually fix one by one

Your task will be accomplished when all lint errors are fixed.
